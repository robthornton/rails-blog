# frozen_string_literal: true

require 'test_helper'

class TheGoodStuffTest < ActionDispatch::IntegrationTest
  test 'the good stuff' do
    # this is where you can check that stuff actually works together, or alternatively in system tests.
    # Given the simplicity of your app, the tests that check if the models can exist on their own aren't super valuable.
    # What's more valuable is "is all this stuff hooked up correctly?"
    # The model unit tests should basically just be checking the validation edge cases and or other custom stuff.
  end
end
